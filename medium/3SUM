import java.util.*;

class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        Arrays.sort(nums);
        List<List<Integer>> res = new ArrayList<>();
        int n = nums.length;

        for (int i = 0; i < n; i++) {
            if (i > 0 && nums[i] == nums[i - 1]) {
                continue;
            }

            int target = -nums[i];
            int left = i + 1;
            int right = n - 1;

            while (left < right) {
                int sum = nums[left] + nums[right];

                if (sum == target) {
                    res.add(Arrays.asList(nums[i], nums[left], nums[right]));
                    left++;
                    right--;

                    while (left < right && nums[left] == nums[left - 1]) {
                        left++;
                    }
                    while (left < right && nums[right] == nums[right + 1]) {
                        right--;
                    }
                } else if (sum < target) {
                    left++;
                } else {
                    right--;
                }
            }
        }

        return res;
    }
}


// суть задачи в том, что мы берем уже 3 ползунка вместо двух( как в задаче на 2sum) первый обычный, второй всегда на единицу больше, чем 
// i, и берем еще один ползунок right, который идет с конца массива.  Исходим и того, что цель у нас такова, что нужно получить сумму второго
// и третьего и сравнить ее с отрицательным итым числом 
// если они равны по модулю, то мы у цели 

// Задача "3Sum": найти все уникальные тройки чисел в массиве,
// сумма которых равна нулю (nums[i] + nums[left] + nums[right] == 0).
//
// Решение основано на сортировке массива и применении модифицированной техники "Two Pointers":
// 1. Сортируем массив: это критически важно для работы указателей и эффективной обработки дубликатов.
// 2. Итерируемся по массиву с помощью одного указателя 'i' (от 0 до n-1).
//    Для каждого nums[i] мы ищем пару (nums[left], nums[right]) такую,
//    чтобы nums[left] + nums[right] == -nums[i].
//    Это эквивалентно задаче "2Sum", где целевая сумма равна -nums[i].
// 3. Используем два дополнительных указателя 'left' (начинается с i + 1)
//    и 'right' (начинается с конца массива n - 1).
// 4. Двигаем 'left' и 'right' навстречу друг другу:
//    - Если сумма nums[left] + nums[right] слишком мала, увеличиваем 'left'.
//    - Если сумма слишком велика, уменьшаем 'right'.
//    - Если сумма равна целевой (-nums[i]), добавляем тройку в результат.
// 5. Особое внимание уделяется пропуску дубликатов, чтобы в результате
//    были только уникальные тройки (как для 'i', так и для 'left'/'right').

